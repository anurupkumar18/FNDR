# Claude Code: FNDR

**FNDR v2 lives in its own repo: https://github.com/anurupkumar18/FNDR-2.0 (local: `~/FNDR-2.0`).** Do v2 work there, in sessions opened inside that repo. This repo is the v1 POC, imported into FNDR-2.0 as the read-only `reference/v1` branch; the historical plan and kickoff stay under `docs/v2/`. The rules below govern changes to this v1 codebase only.

Follow **`AGENTS.md`** in this repository for every task. It defines mandatory defaults and which portable engineering skill to open under `.agent-skills/portable-engineering/` without waiting for the user to name one.
