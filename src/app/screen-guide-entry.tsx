import ReactDOM from "react-dom/client";
import { ScreenGuideOverlay } from "@/domains/screen-guide/ScreenGuideOverlay";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
    <ScreenGuideOverlay />,
);
