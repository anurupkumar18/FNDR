import ReactDOM from "react-dom/client";
import { AutofillOverlay } from "./components/AutofillOverlay";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
    <AutofillOverlay />
);
